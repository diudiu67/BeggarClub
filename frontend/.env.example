# Must match WEB_SECRET in backend .env
VITE_WEB_SECRET=changeme123

# Backend URL (only needed for production builds)
# Local dev: leave empty — Vite proxy handles it
# Production: set to your Railway backend URL
# VITE_API_URL=https://your-backend.railway.app
