# Railway deployment config — installs FFmpeg alongside Python
[phases.setup]
nixPkgs = ["ffmpeg", "python312"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
